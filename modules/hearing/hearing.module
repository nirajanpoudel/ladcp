<?php
function hearing_theme($existing, $type, $theme, $path) {
	// kint($theme);
  return array('hearing' =>
    array(
      'variables' => array(
        'title' => 'Default title', 
        'description' => 'place content goes here',
        'var1'=>'This is var one'
      ),
      'template' => 'block--sampleblock'
    )
  );
}
